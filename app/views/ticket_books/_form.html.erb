<!--<div class="container-fluid">

<div class="col-xs-6 col-xs-offset-5"> -->
<div class="row">
<%= bootstrap_form_for(@ticket_book) do |f| %>
 <% if @ticket_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_book.errors.count, "error") %> prohibited this ticket_book from being saved:</h2>

      <ul>
      <% @ticket_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-xs-3">

 <%= f.text_field :name, placeholder:"enter your name"%>
  
  </div>
  <!--
  <div class="field">
    <%= f.label :is_cancelled %><br>
    <%= f.check_box :is_cancelled %>
  </div>
 
  <div class="field">
    <%= f.label :canceldate %><br>
    <%= f.date_select :canceldate %>
  </div>
  -->
    <%= f.date_select :journeydate%>
 
  <div class="actions">
    <%= f.submit class:'btn' %>
  </div>

<% end %>
</div>
